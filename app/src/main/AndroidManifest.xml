<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:sharedUserId="android.uid.system">

    <!-- Permissions — same as original DriveHub Dort -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- [T-910] Exigée à partir d'API 34 pour foregroundServiceType="connectedDevice". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Consommation de donnees : signature|privileged, accessible car l app est signee
         avec la cle plateforme. Sans elle, NetworkStatsManager leve une SecurityException. -->
    <uses-permission android:name="android.permission.READ_NETWORK_USAGE_HISTORY"
        tools:ignore="ProtectedPermissions" />
    <!-- Pilotage des sessions média (piste suivante, lecture/pause). Voie du launcher
         d'origine sur les firmwares A9, et seule voie pour CarPlay/Android Auto sur
         SWI68/165. signature|privileged : obtenue parce que l'app tourne en
         android.uid.system. -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"
        tools:ignore="ProtectedPermissions" />
    <!-- Vehicle control permissions (Android Automotive / SAIC) -->
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CAR_POWERTRAIN" />
    <uses-permission android:name="android.car.permission.CAR_VENDOR_EXTENSION" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_CLIMATE" />
    <!-- [BT-PROFILES] Bluetooth — lecture des appareils appairés et suivi connexions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- [T-902] "com.saic.keyevent.hardkey.report" n'est pas un broadcast protégé :
         n'importe quelle app installée peut le forger et piloter les raccourcis, donc
         écrire mode de conduite / régen / ADAS / AEB. Le receiver exige désormais cette
         permission côté ÉMETTEUR. En protectionLevel="signature", seule une app signée
         avec la clé plateforme de la ROM l'obtient — c'est le cas de l'émetteur système
         d'origine, jamais d'une app tierce. -->
    <permission
        android:name="com.mg4.control.permission.RECEIVE_HARDKEY"
        android:protectionLevel="signature" />

    <!-- Réseau / auto-update : déclarés UNIQUEMENT dans le flavor online
         (src/online/AndroidManifest.xml). Le flavor offline n'en a aucun, par construction. -->

    <application
        android:name=".MG4App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.MG4Control"
        android:supportsRtl="true">

        <!-- API Car AOSP (CarDoorLockManager pour la détection d'ouverture de porte) -->
        <uses-library android:name="android.car" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- [T-910] foregroundServiceType est OBLIGATOIRE à partir d'API 34 : sans lui,
             startForeground() lève MissingForegroundServiceTypeException. Sans effet sur
             AAOS 9 (API 28), mais le déclarer maintenant évite un crash au premier bump de
             SDK. "connectedDevice" correspond à ce que fait le service : suivre l'état du
             véhicule et les connexions Bluetooth. -->
        <!-- Interception des touches volant (Raccourcis avancés).
             BIND_ACCESSIBILITY_SERVICE est exigée par la plateforme : elle garantit que SEUL
             le système peut se lier à ce service. L'utilisateur doit l'activer lui-même dans
             les réglages d'accessibilité, l'application ne peut pas le faire à sa place. -->
        <service
            android:name=".accessibility.KeyCaptureService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_keycapture" />
        </service>

        <service
            android:name=".service.MG4ControlService"
            android:exported="false"
            android:directBootAware="true"
            android:foregroundServiceType="connectedDevice" />

        <!-- FileProvider + InstallReceiver (auto-update) : déclarés UNIQUEMENT dans
             le flavor online (src/online/AndroidManifest.xml). Déplacer le FileProvider
             hors du manifest de base évite aussi un conflit d'authority qui empêcherait
             d'installer les APK online et offline côte à côte. -->

        <!-- ═══ API externe (issue #79) — KeyMapper, Tasker, etc. ═══════════════
             ⚠️ Exporté SANS permission, et c'est délibéré : KeyMapper et Tasker viennent du
             Play Store, elles ne seront jamais signées avec la clé plateforme, donc une
             permission "signature" les exclurait. Et un broadcast ne porte aucune identité
             d'émetteur : on ne peut pas filtrer l'appelant ici.

             Le verrou est donc AILLEURS, et il est double :
               1. l'interrupteur "API externe" des Réglages, DÉSACTIVÉ PAR DÉFAUT — tant qu'il
                  est off, le receiver sort immédiatement ;
               2. VehicleWriteGate, posé dans les primitives d'écriture de MG4Hardware, donc
                  sous tout point d'entrée : aucune écriture véhicule en roulant.
             Toute commande reçue est journalisée (tag MG4_API), refus compris. -->
        <receiver
            android:name=".api.ExternalApiReceiver"
            android:exported="true">
            <intent-filter>
                <!-- Forme riche : un extra nomme la commande (Tasker, adb, scripts). -->
                <action android:name="com.mg4.control.action.EXECUTE" />
                <action android:name="com.mg4.control.action.SET" />
                <!-- Une action par commande : KeyMapper ne sait saisir QUE la chaine
                     d'action, son editeur d'intent n'a pas de champ extras.
                     Les commandes de securite active (ADAS, AEB, TSR, alertes) et l'extinction
                     vehicule sont VOLONTAIREMENT absentes — cf. ExternalApi.BLOCKED_ACTIONS,
                     qui les refuse aussi via EXECUTE. -->
                <action android:name="com.mg4.control.action.ONE_PEDAL" />
                <action android:name="com.mg4.control.action.ENERGY_SAVING_TOGGLE" />
                <action android:name="com.mg4.control.action.PROFILE_PICKER" />
                <action android:name="com.mg4.control.action.OPEN_APP" />
            </intent-filter>
        </receiver>

        <!-- Lecture seule de l'état véhicule. Contrairement au receiver, un provider CONNAÎT son
             appelant (getCallingPackage) : d'où la liste blanche optionnelle et la journalisation
             nominative. Aucune écriture ne passe par ici. -->
        <!-- Authority dérivée de l'applicationId : les variantes online et offline s'installent
             côte à côte, et deux paquets ne peuvent pas déclarer la même authority
             (INSTALL_FAILED_CONFLICTING_PROVIDER — le FileProvider s'y était déjà heurté).
             Pas de readPermission : Tasker est un APK figé, il ne peut pas déclarer une
             permission que nous inventerions — en exiger une fermerait la porte qu'on ouvre.
             Le contrôle est en code et il est RÉEL ici, contrairement au receiver :
             getCallingPackage() est fiable, donc interrupteur + liste blanche + journal nominatif. -->
        <provider
            android:name=".api.StateProvider"
            android:authorities="${applicationId}.state"
            android:exported="true" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:directBootAware="true"
            android:priority="1000">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
