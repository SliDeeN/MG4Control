<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tvProfileName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/profile_default_name"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            android:drawableEnd="@android:drawable/ic_menu_edit"
            android:drawablePadding="6dp"
            android:paddingVertical="6dp"/>

        <Button
            android:id="@+id/btnSetFavorite"
            style="@style/Widget.MG4Control.Button.Secondary"
            android:layout_width="wrap_content"
            android:layout_height="34dp"
            android:text="@string/btn_set_favorite_off"
            android:paddingHorizontal="10dp"
            android:textSize="11sp"
            android:layout_marginStart="6dp"/>

        <Button
            android:id="@+id/btnApplyProfile"
            style="@style/Widget.MG4Control.Button.DriveMode"
            android:layout_width="wrap_content"
            android:layout_height="34dp"
            android:text="@string/btn_apply_profile"
            android:paddingHorizontal="10dp"
            android:textSize="11sp"
            android:layout_marginStart="6dp"/>
    </LinearLayout>

    <TextView
        android:id="@+id/tvProfileSummary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/text_secondary"
        android:textSize="11sp"
        android:layout_marginTop="2dp"/>

</LinearLayout>
